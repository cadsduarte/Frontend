public class OperadorTernario {
    
    public static void main(String[] args) {
        
        //variável
        double nota = 8;

        String situacao = nota >= 7 ? "Aprovado(a)" : "Reprovado(a)";

        //Operador térnario
        System.out.println(situacao);
    }
}
